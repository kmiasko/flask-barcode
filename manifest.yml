---
applications:
  .:
    name: flask-barcode
    framework:
      name: wsgi
      info:
        mem: 64M
        description: Python WSGI Application
        exec: 
    infra: eu-aws
    url: ${name}.${target-base}
    mem: '256'
    instances: 1
